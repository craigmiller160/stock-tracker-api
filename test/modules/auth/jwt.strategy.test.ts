describe('jwt.strategy', () => {
	describe('validate', () => {
		it('is valid', () => {
			throw new Error();
		});

		it('has invalid client key', () => {
			throw new Error();
		});

		it('has invalid client name', () => {
			throw new Error();
		});
	});
});
